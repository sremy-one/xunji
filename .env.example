# Public runtime configuration only. Never place AppSecret, database passwords,
# COS secret keys, JWT secrets, or SSH credentials in a VITE_* variable.
VITE_API_BASE_URL=https://api.asyore.cn/fitness-api/v1
VITE_API_TIMEOUT_MS=10000

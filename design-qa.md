# 「由迹而寻」银铃训练完成页 QA

验收日期：2026-07-31  
状态：背部·核心组合训练完成，24 分钟，9 组，连续 2 天  
主视口：418×900 CSS px；小屏复核：390×844 CSS px；deviceScaleFactor 1

## 对照证据

- 视觉真值：`C:/Users/HUAWEI/AppData/Local/Temp/codex-clipboard-be78bf04-b1a1-46e1-90ad-4ac0c93401e4.png`（539×955）
- 真值内容裁切：原图 `(83,14)-(502,915)`，419×901，归一化至 418×900
- 实现截图：`design/celebration/implementation-complete-wechat-safe.png`（418×900）
- 小屏截图：`design/celebration/implementation-complete-390x844.png`（390×844）
- 全屏同图对照：`design/celebration/complete-before-after-comparison.png`（872×952）
- 八图聚焦对照：`design/celebration/yinling-finish-eight-posters.jpg`（960×704）
- 图片规范与提示：`design/celebration/celebration-poster-spec.md`

H5 验收截图不绘制微信状态栏和胶囊，因此顶部安全区在实现侧表现为空白；418×900 真值中的系统 UI 不属于应用内容。页面通过 `getMenuButtonBoundingClientRect()` 在微信端计算同一安全区。

## Findings

当前没有可执行的 P0、P1 或 P2 问题。

- 字体与层级：保留宋体主标题、深墨蓝正文和番茄红完成状态；海报没有抢过“每一步，都算数。”的主信息层级。
- 间距与节奏：3:2 海报将原顶部勾选区扩展为纪念画面，其余内容整体下移；418×900 与 390×844 下分享、保存和返回入口仍在首屏内。
- 色彩与令牌：八张插画统一使用暖米白、深墨蓝、雾蓝/淡紫和鼠尾草绿，与首页、选择页和头像一致。
- 图片质量：母版为 1536×1024 PNG，运行时为 960×640 渐进式 JPEG；圆角安全区内没有脸部、手部或主要动作裁切。
- 文案与语义：图内没有生成文字；“训练定格 · 标题”由界面渲染，八张图片均有描述发型、情绪和动作的无障碍文本。
- 随机行为：训练会话 ID 经稳定哈希对 8 取模；新会话近似随机，同一记录重复进入保持同一张。
- 成果海报：点击“保存成果海报”成功生成 620×900 预览，并复用完成页当前银铃插画。
- 空状态：完成页没有会话 ID 或本地记录时显示完整银铃兜底页面，不再因根节点条件渲染产生空白屏。
- 微信分包兼容：完成页的海报配置已输出到各自分包，兜底视图直接编译进页面；不再依赖会被“过滤无依赖文件”误判的新主包组件。

## Comparison History

### 第一轮

- [P1] 宽海报若沿用原有顶部间距，会进入微信胶囊和状态栏区域。
- [P2] 原完成页使用通用圆形勾选，完成情绪弱；主体集中在上半部，底部存在大块未利用空间。
- [P2] 保存成果海报仍使用通用训练素材，与新的银铃品牌叙事不一致。

修复：

- 自定义导航页接入平台安全区；微信端按胶囊底部增加留白，H5 使用保底间距。
- 将圆形勾替换为 3:2 银铃训练定格卡片，并压缩标题、统计、鼓励语之间的间距，形成完整首屏节奏。
- 成果画布使用当前随机银铃海报；署名改为“银铃插画 · 由迹而寻”。

### 第二轮

- 418×900 微信安全区模拟中，海报顶部完整避让，三个操作入口均可见。
- 390×844 小屏中，海报、标题、统计、鼓励语、分享、保存和返回仍全部位于首屏。
- `poster=1`、`poster=2`、`poster=7` 三种发型、动作和构图均正确切换。
- 保存成果海报预览成功，无控制台错误。
- 无新增 P0、P1 或 P2 问题。

## Primary Interactions

- 完成页显示当前会话稳定选中的银铃海报。
- 不同训练会话可映射到八张海报中的任意一张。
- 微信原生分享按钮保持可用。
- H5“保存成果海报”成功打开生成后的海报预览。
- “回到今日”入口保持可用。
- 无会话状态的“挑选今天的训练”成功进入 `/packages/planner/pages/select`。
- 浏览器控制台错误：0。

## Engineering Checks

- TypeScript：通过。
- Vitest：6 个测试文件、18 个测试通过；新增八图唯一性、会话稳定性、视觉预览和双分包配置一致性测试。
- H5：构建通过。
- 微信小程序：构建通过。
- 媒体完整性：8 张母版 PNG、8 张运行时 JPEG、8 张微信构建产物均存在。
- 包体：主包 1.61MB；最大分包 1.02MB；总包 4.62MB；全部通过 2MB/20MB 阈值。

## Follow-up Polish

- P3：可选增强为让海报情绪与实际训练强度关联，例如高训练量优先出现喘气/躺下恢复，短时训练优先出现比耶/记录。

final result: passed
